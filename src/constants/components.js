import  {APP_WIDGET_CONTAINER_PREFIX} from "./app";

/**
 * Widget container for standalone components.
 *
 * - Datetime picker
 *
 * @type {string}
 */
export const UNIFIED_CONTAINER_DATETIME_PICKER = `${APP_WIDGET_CONTAINER_PREFIX}-datetime-picker`;
